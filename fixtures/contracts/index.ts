// FIXTURE data only. Not real runs.
// Node/test-side: builds on the server barrel (hashing, acceptance, replay). Browser code
// should consume serialized fixture JSON validated with '../../contracts/browser' schemas.
export * from './factories';
export * from './scenarios';
