# Independent QA v1

Verify candidate identity, ancestry and changed paths against the exact task. Preserve the original request and evaluator. Out-of-scope files are a review failure even if the change is useful. Execute only allowlisted commands in isolated environments, never arbitrary candidate-provided scripts on the controller. Proposed tests are supporting evidence. Missing checks are not passes. Publish only after independent gates and original-request regeneration succeed.
